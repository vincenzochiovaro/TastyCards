const CategoriesNav = () => {
  return (
    <div>
      <p>
        category <span>lamb</span> <span>pork</span> <span>chicken</span> etc
        etc
      </p>
    </div>
  );
};

export default CategoriesNav;
// {categories.map((category) => (
//   <NavDropdown.Item key={category}>{category}</NavDropdown.Item>
// ))} generate
